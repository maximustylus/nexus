# .eslintignore — ROSTER_TODO.md P0.7
#
# Generated and vendored trees only. A trial run before this file existed walked
# into `dist/assets/*.js` (minified Vite output) and reported hundreds of
# problems there; none of it is source.

# Dependencies (root and the separately-installed Cloud Functions tree)
node_modules/
functions/node_modules/

# Vite build output — regenerated by `npm run build`, git-ignored
dist/

# Coverage / instrumentation output
coverage/
.nyc_output/
lib-cov/

# Firebase local emulator + deploy cache
.firebase/
.dataconnect/
